<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Premium Software | Log</title>
    <link rel="stylesheet" href="/public/assets/bootstrap.min.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg fixed-top navbar-secondary bg-primary">
        <div class="container d-flex flex-column">
            <div class="container d-flex flex-row justify-content-between">
                <h2 class="text-light"> {{log.logFile}} </h2>
                <div class="form-group">
                    <div class="input-group mb-3">
                        <input type="date" class="form-control" id="search" />
                        <button class="btn btn-dark" type="button" id="btn-send">Pesquisar</button>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <div class="container" style="margin-top: 100px;">
        <table class="table table-hover">
            <tbody>
                {{#each log.items}}
                <tr level={{level}}>
                    <td class="align-middle col-2 timestamp" style="font-size: 13px;">
                        {{timestamp}}
                    </td>
                    <td class="align-middle col-1 level" style="font-size: 13px;">
                        {{level}}
                    </td>
                    <td class="align-middle col-4 message" style="font-size: 13px;">
                        {{message}}
                    </td>
                    <td class="align-middle col-5">
                        <div class="form-group">
                            <textarea class="form-control metadata" rows="4"
                                style="font-size: 13px;">{{metadata}}</textarea>
                        </div>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <script src="/public/assets/global.js"></script>
</body>

</html>